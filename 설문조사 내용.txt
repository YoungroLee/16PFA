소숫점아래 표시와 관련잇는 명령어
%f %e(범위가 넓은 숫자) %g


문자열을 변수에 저장할 수 있다.[필수][1개 선택]

1) True 


문자열 안에 구별할 수 있는 표시를 해 두면 그 자리에 다른 문자열을 넣을 수 있다.[필수][1개 선택]
1) True

3. 문자열 안 여러 장소에 표시를 해 두면 여러 변수 값이 문자열 안에 표시되도록 할 수 있다.[필수][1개 선택]
True

4. 문자열 안에 표시 되도록 하기 위해서는 계산 결과가 사전에 어떤 변수에 저장되어야만 한다.[필수][1개 선택]
1) True

. 다음 중 문자열과 관계 있는 것은?[필수][1~5개 선택
 %s

다음 중 정수와 관련된 것은?[필수][1
2) %d
문자열 연산 결과를 어떤 변수에 저장할수있다. 
True

%s와 %r 가운데 어느 쪽에 더많은 정보를 기대할 수 있는가?

%r

python 에서 문자열 + 문자열 연산을 할 수 있다.
ture

pyhon 에서는 문자열 % 문자열 연산의 규칙이 정해져 있어 가능하다
True

\n